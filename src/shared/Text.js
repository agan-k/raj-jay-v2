import styled from 'styled-components';
import {
  space, 
  color, 
  typography,
  layout,
} from 'styled-system';

const Text = styled('div')(
  space,
  color,
  typography,
  layout
);
export default Text;
