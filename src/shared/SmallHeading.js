import styled from 'styled-components';
import {
  space, 
  color, 
  typography,
  layout,
} from 'styled-system';

const SmallHeading = styled('h2')(
  space,
  color,
  typography,
  layout
);
export default SmallHeading;
