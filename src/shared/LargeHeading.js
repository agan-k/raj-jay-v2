import styled from 'styled-components';
import {
  space, 
  color, 
  typography,
  layout,
} from 'styled-system';

const LargeHeading = styled('h1')(
  space,
  color,
  typography,
  layout
);
export default LargeHeading;
