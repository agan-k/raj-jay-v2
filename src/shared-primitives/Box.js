import styled from 'styled-components';
import {space, layout, color, flexbox, typography, border, style} from 'styled-system';

const Box = styled('div')(
  space,
  layout,
  color,
  flexbox,
  typography,
  border
);
export default Box;